---
AIGC:
    Label: "1"
    ContentProducer: 001191110102MACQD9K64018705
    ProduceID: 7635505238425895209-data_volume/files/所有对话/主对话/PRD.md
    ReservedCode1: ""
    ContentPropagator: 001191110102MACQD9K64028705
    PropagateID: 1707989516438281#1788400807266
    ReservedCode2: ""
---
# TravelWise 产品需求文档 (PRD)

> **文档定位**：本 PRD 基于现有实现进行补充与梳理，用于指导 TravelWise 后续迭代，并作为项目仓库中的产品规格说明。  
> **版本**：V1.0  
> **日期**：2026-09-03  
> **维护者**：张广鑫

---

## 1. 项目背景

### 1.1 产品定位

TravelWise 是一个**平台无关、开箱即跑、带评测集的出行决策 Agent**。

它不是聊天机器人壳子，而是一套完整的 Agent 工程实践：意图路由、参数抽取、范围控制、工具调用、失败降级、人工确认闸门，以及一套**可回归运行的评测集**。

### 1.2 解决什么问题

出行规划里有两件事最耗神：

1. **机票蹲守**：用户真正耗神的不是「查」，而是「每天打开看有没有降价」的持续蹲守。
2. **流量噪声**：现有攻略普遍是「互联网声量大就推荐什么」，缺少季节与人流的考量。

TravelWise 把这两件事自动化，让用户从多平台、多天的碎片化决策，压缩到一次自然语言提问。

### 1.3 核心用户

| 用户分层 | 画像 | 核心诉求 |
|---|---|---|
| **核心用户** | 自由行爱好者、学生、预算敏感旅行者 | 想知道「机票提前几天买更划算」，减少无意义蹲守 |
| **扩展用户** | 数字游民、灵活出行者 | 目的地探索，避开流量推荐，找到季节合适的去处 |
| **潜在用户** | 旅游内容创作者、旅行规划工具开发者 | 可复用的 Agent 架构、评测集和 Provider 抽象 |

> 注：TravelWise 当前聚焦于**个人出行决策场景**，不面向企业差旅报销场景。

---

## 2. 需求概述

### 2.1 用户痛点

| 痛点 | 现状 | TravelWise 解法 |
|---|---|---|
| 机票蹲守 | 平台把比价成本转嫁给个人，用户每天打开 App 看价格 | 横向扫描近期出发日，读出「价格～提前天数」形状，输出建议购票日 |
| 流量噪声 | 攻略被声量绑架，忽略季节与人流 | 以官方 A 级景区名录为候选池，二层场景发现收敛非官方打卡地 |
| 决策碎片化 | 机票和目的地信息分属不同平台 | 一次提问同时输出机票分析 + 目的地推荐 |
| 黑箱不可信 | 普通 Agent 出错后无法定位 | 显式 TravelState + 调用链追踪，每次运行可审计 |

### 2.2 业务价值

- 缩短用户出行决策时间，从「多天多平台」到「一次提问」。
- 通过「部分执行」机制，保证交付完整性，一个模块失败不拖垮全局。
- 通过评测集让 Agent 行为从「感觉更好」变成「数据可证」。

### 2.3 版本范围

**V1.0（当前已实现）**

- 意图路由与参数抽取
- 机票购买时机分析
- 目的地推荐
- 多轮对话续跑
- 购票提醒（ICS 日历）
- 调用链追踪
- 351 项单元测试 + 51 条评测用例

**V2.0（规划中）**

- Persistent Memory（跨 Session 偏好）
- 目的地 RAG 语义检索
- 多语言支持

**V3.0（远期）**

- 自建 MCP Server
- LangGraph 工作流建模
- Multi-Agent 协作

---

## 3. 用户需求与功能需求

### 3.1 用户需求

| 用户角色 | 使用场景 | 核心需求 |
|---|---|---|
| 预算敏感旅行者 | 「8月28号从上海飞成都，机票什么时候买划算」 | 获得基于数据的购票时机建议，而不是「提前两周」的模糊说法 |
| 目的地探索者 | 「沈阳有什么好玩的」 | 获得去噪声的目的地清单，含季节适宜性和来源链接 |
| 综合规划者 | 「月底飞乌鲁木齐，新疆有什么玩的」 | 一次提问同时获得机票分析 + 目的地推荐 |
| 多轮对话用户 | 先问机票，再追问目的地 | 对话状态保持，第二问能接住第一问上下文 |
| 开发者/研究者 | 跑评测、看 trace | 可复现、可审计、可扩展的 Agent 工程参考 |

### 3.2 功能需求

#### P0（核心已交付）

- **意图路由**：识别纯机票 / 纯目的地 / 混合 / 超范围四类意图。
- **参数抽取**：提取出发地、目的地、出行日期，缺失时标记而不是瞎猜。
- **机票购买时机分析**：多算法对照（逐航班共识、面板中位价、全量最低价）。
- **目的地推荐**：A 级景区名录 + 二层场景发现。
- **部分执行**：单 Skill 失败不拖垮全局。
- **失败降级**：工具失败时明确报错，不编造价格或结果。
- **规则路由基线**：零模型成本的可复现基线。

#### P1（已交付增强）

- **LLM 路由**：参数抽取更鲁棒，与规则路由跑同一套评测可对照。
- **Agent Loop**：模型自主选工具，最灵活但成本最高。
- **多轮对话续跑**：状态落盘，跨进程 `--session` 继续。
- **人工确认闸门**：副作用操作先预览后确认。
- **ICS 购票提醒**：生成标准日历文件。
- **调用链追踪**：`--trace` 记录并渲染为 HTML。

#### P2（可选增强）

- **JSON 结构化输出**：便于接入其他系统。
- **Mock 模式**：无 API Key 也能跑通全流程。

---

## 4. 业务流程

### 4.1 整体流程

```
用户自然语言请求
    │
    ▼
┌─────────────────┐
│ Router          │  意图路由 / 参数抽取 / 范围判断
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ TravelState     │  显式任务状态（可 dump、可断言）
└────────┬────────┘
         │
    ┌────┴────┐
    ▼         ▼
┌───────┐ ┌───────────┐
│Flight │ │Destination│
│Skill  │ │Skill      │
└───┬───┘ └─────┬─────┘
    │           │
    └─────┬─────┘
          ▼
   Orchestrator Merge
          │
   需要副作用操作？
    ╱        ╲
  No         Yes
   │           │
  END    Preview → 用户确认 → 执行
```

### 4.2 正常流程

1. 用户输入自然语言请求。
2. Router 解析意图，识别为 flight / destination / 混合 / 超范围。
3. 抽取参数，缺失时标记 `missing`。
4. Orchestrator 调用对应 Skill。
5. 各 Skill 返回结构化结果。
6. Orchestrator 合并结果，生成统一回复。
7. 若涉及副作用（如设提醒），先预览并请求用户确认。
8. 交付最终结果。

### 4.3 异常流程

| 异常场景 | 处理方式 |
|---|---|
| 航班 API 超时/不可用 | `flight_result.ok = False`，不编造数据；目的地清单正常交付 |
| 搜索 API 不可用 | 二层场景发现降级，仅输出 A 级景区名录，并明确标注 |
| LLM API 不可用 | 降级为 Rule Router，保证基础功能可用 |
| 用户输入超出范围 | 明确拒绝，不用相近能力硬凑 |
| 参数缺失 | 标记缺失并追问，但已可执行部分先交付 |
| 用户取消副作用 | 不执行，trace 中不留 `reminder.create` 事件 |

### 4.4 状态流转

```
IDLE → ROUTING → DISPATCHING → PARTIAL_COMPLETE / COMPLETE / FAILED
                    │
                    ├── 有副作用 → AWAITING_APPROVAL → APPROVED / REJECTED
                    │                                        │
                    │                                   EXECUTING → DONE
                    │
                    └── 无副作用 → DONE
```

---

## 5. 功能范围

### 5.1 In Scope（当前已实现）

- ✅ 意图路由（规则 + LLM + Agent Loop 三种模式）
- ✅ 机票购买时机分析（多算法对照）
- ✅ 目的地推荐（A 级景区名录 + 二层场景发现）
- ✅ 参数抽取与缺失追问
- ✅ 部分执行（单失败不连坐）
- ✅ 失败降级与明确报错
- ✅ 多轮对话续跑
- ✅ 人工确认闸门
- ✅ ICS 购票提醒
- ✅ 调用链追踪（JSONL + HTML 渲染）
- ✅ 351 项单元测试
- ✅ 32 条回归闸门 + 19 条难例 + 9 项 Agent 指标

### 5.2 Out of Scope（暂不做）

- ❌ 酒店、火车票、租车等非机票出行方式
- ❌ 完整 RAG（无 chunking / embedding / 向量库）
- ❌ 跨 Session 持久化 Memory
- ❌ 自建 MCP Server
- ❌ LangGraph / Multi-Agent
- ❌ 多语言支持（当前仅中文）
- ❌ 移动端原生 App

---

## 6. 功能设计

### 6.1 关键设计决策

| 设计点 | 决策 | 理由 |
|---|---|---|
| Provider 抽象层 | 所有外部依赖在 Provider 接口之后 | 换数据源只换实现，业务逻辑不动；无 Key 用 Mock 照样跑 |
| 三种运行模式并存 | Rule Router / LLM Router / Agent Loop | 没有基线，「上 LLM 更好」就只是感觉；三者跑同一套评测可对照 |
| 禁止假成功 | 工具失败必须明确失败，绝不编造 | 安全红线，Agent 评测中单独考核 |
| 部分执行 | 缺参数只挡对应 Skill | 缺出发地时先把目的地清单给出，同时追问出发地 |
| 难例组不进闸门 | 期望值写正确行为，不是当前行为 | 避免闸门长期红着被 disable，也避免评测集降格成快照 |
| 零第三方依赖 | Python 标准库完成全部运行时 | clone 下来即可跑，降低使用门槛 |

### 6.2 技术方案

- **语言**：Python 3.10+
- **依赖**：零第三方依赖（纯标准库）
- **LLM**：DeepSeek API（OpenAI 兼容协议），可替换为其他厂商
- **航班数据**：HTTP Provider 配置驱动，默认接入阿里云机票价格 API
- **搜索**：HTTP Provider 配置驱动，用于二层场景发现
- **提醒**：console / ics / json / mcp 四种 Provider
- **测试**：unittest + 自定义评测框架
- **追踪**：JSONL 落盘，脚本渲染为单文件 HTML

---

## 7. 数据埋点

### 7.1 埋点事件

| 事件名称 | 说明 | 触发条件 | 关键参数 |
|---|---|---|---|
| `route_intent` | 意图路由结果 | Router 完成分类 | `intents[]`, `router_type` |
| `skill_execute` | 技能执行 | Skill 开始执行 | `skill_name`, `params` |
| `skill_complete` | 技能完成 | Skill 返回结果 | `skill_name`, `ok`, `duration_ms` |
| `partial_delivery` | 部分交付 | 一个 Skill 失败但另一个成功 | `delivered[]`, `failed[]` |
| `hitl_prompt` | 人工确认请求 | 副作用触发确认闸门 | `action_type`, `preview_content` |
| `hitl_decision` | 人工确认结果 | 用户确认/拒绝 | `decision`, `action_type` |
| `reminder_create` | 提醒创建 | 购票提醒成功生成 | `reminder_type`, `format` |

### 7.2 关键指标

| 指标 | 目标 | 说明 |
|---|---|---|
| 意图路由准确率 | ≥ 85% | 回归闸门 + 难例组综合评估 |
| 技能执行成功率 | ≥ 90% | 单元测试 + 真实 API 调用综合评估 |
| 部分交付覆盖率 | ≥ 95% | 单失败场景下仍能交付可用结果 |
| 失败恢复率 | ≥ 90% | 工具失败后仍能给出可交付信息 |
| 不编造通过率 | 100% | 红线指标，工具失败时不编造数据 |

---

## 8. 性能指标

| 指标 | 目标值 | 说明 |
|---|---|---|
| 端到端响应时间（P95） | < 15s | 从用户提问到完整回复 |
| LLM 路由延迟（P95） | < 3s | Router + 参数抽取 |
| 航班 API 响应时间（P95） | < 5s | 单次航班查询 |
| 技能执行成功率 | > 90% | skill_complete.ok / skill_execute 总数 |
| API 降级触发率 | < 5% | 外部 API 不可用导致的降级 |
| Token 消耗（单次） | < 8000 | LLM 调用总 token 数 |

### 降级策略

- LLM API 不可用 → 降级为 Rule Router
- 航班 API 不可用 → 仅交付目的地推荐
- 搜索 API 不可用 → 仅交付 A 级景区名录
- tools不可用 → 返回错误信息

---

## 9. 风险与异常

### 9.1 风险列表

| 风险类型 | 风险描述 | 等级 | 应对措施 |
|---|---|---|---|
| 技术风险 | 外部 API（航班/搜索/LLM）不稳定或限流 | 高 | 15s 超时；Mock 模式兜底；失败降级不编造；缓存减少重复调用 |
| 业务风险 | 机票分析结果与实际价格偏差引发用户不信任 | 中 | 明确标注「启发式分析，非精确预测」；提供多算法对照；免责声明前置 |
| 数据风险 | 景区名录、门票、开放时间随时间变化 | 中 | 在输出中标注「仅供参考，以官方公告为准」；Roadmap 计划补 `updated_at` |
| 数据风险 | API Key 泄露 | 低 | Key 只从环境变量读取；仓库中只有 `.env.example` 模板 |
| 合规风险 | 数据文件来源未全部附 URL 与核验时间 | 低 | 已在 Roadmap 中规划补 `source` / `verified_at` |

### 9.2 边界条件

| 维度 | 边界 |
|---|---|
| 业务边界 | 仅支持中文输入；仅支持机票和目的地两类任务；当前聚焦国内场景 |
| 技术边界 | 单次查询扫描窗口 7-14 天；单次最多 14 次航班 API 调用；LLM 单次 token 上限 8K |
| 数据边界 | A 级景区名录约 5000+ 条；城市三字码约 200+ 个国内城市 |

---

## 10. 待确认与后续规划

### 10.1 待确认事项

| 事项 | 状态 | 说明 |
|---|---|---|
| 真实 API 供应商选择 | 待定 | 当前以阿里云机票 API 为例，可替换为其他供应商 |
| 难例组目标 | 进行中 | 当前规则路由 4/19，LLM 路由需系统对照实验 |
| 数据溯源 | 进行中 | 为 `data/source/` 下数据补 `source` / `updated_at` / `verified_at` |
| RAG 引入时机 | 评估中 | V2.0 考虑加入语义检索，与结构化检索共存 |

### 10.2 路线图

| 版本 | 核心特性 | 预计时间 |
|---|---|---|
| V1.0 | 当前已实现：机票分析 + 目的地推荐 + 评测体系 | 已交付 |
| V1.1 | 真实模型对照实验、用户反馈驱动的体验优化 | 2026 Q4 |
| V2.0 | Persistent Memory + 目的地 RAG + 多语言支持 | 2027 Q1 |
| V3.0 | 自建 MCP Server + LangGraph 工作流 + Multi-Agent | 2027 Q2 |

### 10.3 近期行动项

- [√] 完成一次带真实 API Key 的端到端运行，更新 `docs/real-model-runs.md`
- [√] 为 `data/source/` 数据补充来源与核验时间
- [ ] 补充更多难例，验证 LLM 路由相比规则路由的真实提升
- [ ] 完善多轮对话的边界测试（歧义、显式覆盖、听不懂）
- [ ] 评估是否引入可选的第三方依赖（如 OpenAI SDK）以简化真机接入

---

## 附录：项目结构速查

```
travelwise-agent/
├── README.md · LICENSE · pyproject.toml · requirements.txt
├── .env.example · .gitignore
├── docs/                    # 业务规则、架构、ADR
├── prompts/                 # 平台无关的 Agent 指令
├── src/travelwise/          # 核心代码
│   ├── router.py            # 意图路由
│   ├── orchestrator.py      # 调度 / 合并 / HITL
│   ├── state.py             # TravelState
│   ├── session.py           # 多轮会话
│   ├── tracing.py           # 调用链追踪
│   ├── skills/              # Flight / Destination
│   ├── tools/               # 确定性计算
│   ├── providers/           # 外部依赖抽象
│   └── analytics/           # 历史价格分析
├── data/source/             # A 级景区名录、城市三字码
├── config/                  # API 配置模板
├── evals/                   # 评测集
└── tests/                   # 单元测试
```

---

> **License**：本项目采用 PolyForm Noncommercial License 1.0.0，允许个人学习、研究、教学与非商业使用。商业授权请联系作者。

---

> 本内容由 Coze AI 生成，请遵循相关法律法规及《人工智能生成合成内容标识办法》使用与传播。
